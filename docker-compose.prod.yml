version: '3.8'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - VITE_SUPABASE_URL=${VITE_SUPABASE_URL:-}
        - VITE_SUPABASE_ANON_KEY=${VITE_SUPABASE_ANON_KEY:-}
    container_name: qwalify_frontend
    restart: unless-stopped
    networks:
      - root_default
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=root_default"
      - "traefik.http.routers.qwalify.rule=Host(`qwalify.online`) || Host(`www.qwalify.online`)"
      - "traefik.http.routers.qwalify.entrypoints=websecure"
      - "traefik.http.routers.qwalify.tls=true"
      - "traefik.http.routers.qwalify.tls.certresolver=letsencrypt"
      - "traefik.http.services.qwalify.loadbalancer.server.port=80"
      - "traefik.http.routers.qwalify-http.rule=Host(`qwalify.online`) || Host(`www.qwalify.online`)"
      - "traefik.http.routers.qwalify-http.entrypoints=web"
      - "traefik.http.routers.qwalify-http.middlewares=redirect-to-https"

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: qwalify_backend
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - root_default
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=root_default"
      - "traefik.http.routers.qwalify-backend.rule=(Host(`qwalify.online`) || Host(`www.qwalify.online`)) && (PathPrefix(`/webhook`) || PathPrefix(`/api`))"
      - "traefik.http.routers.qwalify-backend.entrypoints=websecure"
      - "traefik.http.routers.qwalify-backend.tls=true"
      - "traefik.http.routers.qwalify-backend.tls.certresolver=letsencrypt"
      - "traefik.http.services.qwalify-backend.loadbalancer.server.port=3001"

networks:
  root_default:
    external: true
